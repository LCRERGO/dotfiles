#!/usr/env sh

# User variables
export TERMINAL="st-256color"
export EDITOR="nvim"
export BROWSER="brave-browser"
export FILE_BROWSER="ranger"
export MUSIC_PLAYER="ncmpcpp"
export CALENDAR="calcurse"
export CALCULATOR="ghci"
export IRC_CLIENT="irssi \
    --config=${XDG_CONFIG_HOME}/irssi/config \
    --home=${XDG_DATA_HOME}/irssi"
export SCRIPTS="${HOME}/.local/share/scripts"
export SOURCE="${HOME}/.local/src"
export HISTCONTROL="ignoreboth"
export DEBEMAIL='lcr.ergo@gmail.com'

# XDG variables
export XDG_CONFIG_HOME="${HOME}/.config"
export XDG_CACHE_HOME="${HOME}/.cache"
export XDG_DATA_HOME="${HOME}/.local/share"
export XDG_STATE_HOME="${HOME}/.local/state"
export XDG_CURRENT_DESKTOP="Njord DE"

# General Configuration
# FZF Options
export FZF_DEFAULT_OPTS="--reverse"
# Fixing Java
export _JAVA_AWT_WM_NONREPARENTING=1
# colored GCC warnings and errors
export GCC_COLORS="error=01;31:\
    warning=01;35:\
    note=01;36:\
    caret=01;32:\
    locus=01:\
    quote=01"
# Libvirt connection setup
export LIBVIRT_DEFAULT_URI="qemu:///system"
# Vagrant default provider
export VAGRANT_DEFAULT_PROVIDER="libvirt"
# Adding cargo binaries to the PATH
export PATH="${PATH}:${XDG_DATA_HOME}/cargo/bin"
# Adding go binaries to the PATH
export PATH="${PATH}:${XDG_DATA_HOME}/go/bin"
# Emacs org directory
export ORG="${XDG_DATA_HOME}/org"
# GDB history file
export GDBHISTFILE="${XDG_DATA_HOME}/gdb/history"

# Cleanup variables
# Password Store Directory
export PASSWORD_STORE_DIR="${XDG_DATA_HOME}/pass"
# Node configuration file
export NPM_CONFIG_USERCONFIG="${XDG_CONFIG_HOME}/npm/npmrc"
# Less cleanup
export LESSKEY="${XDG_CONFIG_HOME}/less/lesskey"
export LESSHISTFILE="${XDG_CACHE_HOME}/less/history"
# Readline Wrapper history
export RLWRAP_HOME="${XDG_DATA_HOME}/rlwrap"
# Stack directory
export STACK_ROOT="${XDG_DATA_HOME}/stack"
# Rust directory
export RUSTUP_HOME="${XDG_DATA_HOME}/rustup"
export CARGO_HOME="${XDG_DATA_HOME}/cargo"
# Go directory
export GOPATH="${XDG_DATA_HOME}/go"
# Vagrant directories
export VAGRANT_HOME="${XDG_DATA_HOME}/vagrant"
export VAGRANT_ALIAS_FILE="${XDG_DATA_HOME}/vagrant/aliases"
# Docker directory
export DOCKER_CONFIG="${XDG_CONFIG_HOME}/docker"
# GTK directories
export GTK2_RC_FILES="${XDG_CONFIG_HOME}/gtk-2.0/gtkrc"
# Jupyter directories
export IPYTHONDIR="${XDG_CONFIG_HOME}/jupyter"
export JUPYTER_CONFIG_DIR="${XDG_CONFIG_HOME}/jupyter"
# Ruby Gem directories
export GEM_HOME="$XDG_DATA_HOME"/gem
export GEM_SPEC_CACHE="$XDG_CACHE_HOME"/gem
# AWS files
export AWS_SHARED_CREDENTIALS_FILE="${XDG_CONFIG_HOME}/aws/credentials"
export AWS_CONFIG_FILE="${XDG_CONFIG_HOME}/aws/config"
