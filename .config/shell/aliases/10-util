# Utilities aliases
alias \
    e="emacs" \
    ec="emacsclient" \
    g="git" \
    s="batcat" \
    dk="docker"  \
    docker-compose="podman-compose"\
    dkc="docker-compose"\
    vm="virsh" \

# Assumes the given alias is already created
#docker="podman"
